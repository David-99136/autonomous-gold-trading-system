# 本機稽核日報第一階段

Status: resolved
Type: task

承接主規格 §14，只讀現有 SQLite events，提供明確起訖（含時區，左閉右開）的
事件摘要與 Markdown 報告，不連券商、不呼叫模型、不啟動排程。

Paper 用 simulated_at；Demo 用事件記錄時間（不是成交時間）。模式由操作者指定，
不當作已驗證帳戶身分。既有 Paper EXIT 缺完整交易 ID，因此只計平倉分段與分段損益，
不計勝率／完整交易數。Demo 未接成交帳本，損益與部位不得由空事件推定為零。
完整成本、R、浮虧等缺資料欄位標示未知。報告永遠暫結，不充當正式帳務驗收。

驗證：時間界線、Paper／Demo 隔離、部分平倉、空資料、壞資料、唯讀開啟、
不覆寫既有輸出及不輸出原始 payload／帳戶識別資料。

## Answer

`gold_system/reporting.py` 與 CLI `daily-report` 完成此階段。新增 10 項測試，
完整 209 項通過；CLI 端到端測試確認不讀取券商憑證。
完整成交帳本、日終排程、即時事件推送與持久化通知去重不在本階段完成範圍。
