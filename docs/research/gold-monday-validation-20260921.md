# 星期一 GOLD 唯讀驗證：行情可用，策略未通過資格驗收

日期：2026-09-21，實測約 Asia/Taipei 10:16–10:20（UTC 02:16–02:20）。
版本：`c4975f2`。範圍：Capital.com **Demo GOLD** 行情與歷史資料、事後價格原型。
未呼叫模型、未下單、未改動交易鎖、未啟動常駐或 Live。不是正式 Demo 資格交易日。

## 實測結果

| 檢查 | 結果 | 證據邊界 |
|---|---|---|
| Demo 市場狀態 | TRADEABLE，商品 GOLD／USD | 不等於通過策略或高流動性時段檢查 |
| 短期 WebSocket | 20 秒診斷正常到期，69 筆有效報價 | 只有約 18.17 秒首末報價跨度，不是長期穩定性證明 |
| 報價價差 | 0.50–0.75 USD；最大本機接收間隔 1.670031 秒 | 不替代 20 日價差基準或完整送單延遲 |
| 券商時鐘估計 | 偏差 +362.21 ms，不確定度 133.00 ms | 短期估計，不更動作業系統時鐘 |
| M1 初次下載 | 256 根，UTC 9/20 22:00 至 9/21 02:15 標記；不含請求終點 02:16 | 無分鐘缺口、無無效 OHLC、無 bid/ask 交叉；內部雜湊核對通過，不是券商簽章 |
| M5／H1 對齊 | 視標記為開棒時，51 根 M5、4 根 H1 的 bid 和 ask 均逐欄完全吻合 | 僅本次窗口的實證支持，不宣稱 API 的永久 finality 保證 |
| 最近 K 線重取 | 第一份有交叉與 ask 修訂，後續穩定 | **未通過近期歷史品質／收棒後不可變性驗收** |

將 M1 原始時間直接當作收棒端點時，可比較的 51 根 M5／4 根 H1 均沒有完整 OHLC
匹配；將 M1 標記加 1 分鐘、原生 M5/H1 分別加 5/60 分鐘後，兩側均全部匹配。
這是本次資料支持「標記為開棒時間」的推論，沒有修改正式 feed adapter，
也沒有把 `closure_verified` 設為 true。

## 近期 ask 異常：保留失敗證據

首次最近資料收到時間 UTC 02:18:02.605974；範圍為 from=02:15、未指定 to。
02:15、02:16 的部分 ask OHLC 低於對應 bid。
例如 02:15 的 close bid=4367.74，ask=4367.73；約 31 秒後 ask 變成 4368.49，bid 不變。
02:15、02:16、02:17 三根的 ask 均有變動，不能簡化成「只有當前未收棒在動」。

第二次收到時間 UTC 02:18:33.522558，沒有交叉。
接著固定 from=02:10、to=02:17，在 02:19:56.855735 與 02:20:07.814880 重取：
各 8 根，正規化 response 內容完全一致，無交叉，與第二份最近資料的重疊棒一致。
包裝檔包含不同接收時間，因此檔案雜湊不同不代表價格不同。

目前只證明原始回應存在暫時異常；未確定是快取、bid/ask 更新同步或查詢終點的影響。
固定窗口成功不代表根因已定位，也不能直接選一個等待秒數就宣稱 finality。
原始快照各自保留，不覆寫、不以正常後值替換早期決策可見值。

## 事後價格原型測試，不是策略績效

僅用初次完整的 256 根 M1 bid，採上述開棒標記假設轉為收棒端點，逐分鐘累積，
以既有 `replay.aggregate` 建立 M5/H1，供 `analysis.analyze` 的未校準價格原型使用。
每個候選只用當時切片的棒；但資料本身是事後下載，**不構成 point-in-time 證據**。

達到至少 3 根 H1、12 根 M5、2 根 M1 後，共 77 個可評估端點：

- NO_TRADE：73。
- SHORT 候選：4（右側延續 2、左側反轉 2）。
- 未模擬成交；候選時點不等於獨立交易、沒有勝率／PF／盈虧結果。

沒有使用異常最近資料；没有把候選送進模型、風控或券商。宏觀證據、完整訊號契約、
成本與高流動性時段均未完成核對，因此不輸出可執行交易建議。
本次時間也未證明屬於規格要求的 London/New York 核准高流動性時段。

## 本機檔案與重現

原始資料與一次性唯讀腳本均在已忽略的 `runtime/`，不推送原始資料或憑證：

- `gold-monday-20260921-market-0215.json`
- `gold-monday-20260921-stream-0215.jsonl`
- `gold-monday-20260921-bars-0220/m1/`：原始 page、合併 prices 與 manifest。
- 同目錄 `MINUTE_5.json`、`HOUR.json`、`recent-before.json`、`recent-after.json`、`fixed-repeat-0.json`、`fixed-repeat-1.json`。
- `gold_monday_bars_probe.py`、`gold_monday_fixed_probe.py`：固定 Demo，只讀價格，不含訂單呼叫；限定當次日期／新檔，不能當作正式 feed 重跑覆寫。

M1 原始 page SHA-256：`91d8c97b36599dcea35ec0d254d1ff8c3851479fe82def93ee0aafb198cfe50e`。
異常 recent-before SHA-256：`c913d311d34649733ef83d9bcaa08c99ece483e8201f5005ce6af27dd7adf7a3`。
後續 recent-after SHA-256：`453e6b8fdb7f8fe5509f29ffce9882a061cca161f25ec70c0d03332ada00b20d`。

离線完整性重查：

```powershell
.\run.ps1 history-audit --directory runtime/gold-monday-20260921-bars-0220/m1
```

既有多週期 14 項及串流 8 項測試通過，共 22 項；本輪未重跑全套 283 項。
Windows 沙箱讀不到憑證，但使用者環境能正常讀取並成功登入；未重設憑證或更動程式。

## 下一步與阻擋条件

先針對固定窗口、分鐘邊界與雙側完整性設計可重複測試，再建立正式 REST 轉換／
修訂偵測與恢復處理；需另行實作及驗收，不能偷偷放寬新鮮度或忽略 ask 異常。
其他未完成項包括完整訊號契約、宏觀 PIT 證據、價差基準與契約值、風控接線及 OOS。
因此本輪結論為：**市場連線短測成功，近期 K 線品質失敗，策略尚未具備 Demo 自動交易資格。**

文件來源：[Capital.com 官方 API](https://open-api.capital.com/)，2026-09-21 查閱。
文件提供 Demo endpoint、M1/M5/H1 及 bid/ask OHLC，並以 snapshotTimeUTC 篩選；
本次查閱的 Prices 說明未提供足以排除上述修訂問題的明確 finality 保證。
